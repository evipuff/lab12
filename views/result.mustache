<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Result</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css" />

    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #f0f4f8;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .main__container {
            max-width: 900px;
            margin: auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 16px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }
        h2 {
            text-align: center;
            color: #ff6f61; /* Soft pink */
            margin-bottom: 10px;
        }
        h3 {
            text-align: center;
            color: #555;
            margin-bottom: 30px;
        }
        h1 {
            text-align: center;
            color: #ff4b5c; /* Soft coral */
            margin-bottom: 40px;
            font-size: 2.5rem;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }
        th {
            background-color: #ff6f61; /* Soft pink */
            color: white;
            border-radius: 8px 8px 0 0;
        }
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        tr:hover {
            background-color: #ffe0e6; /* Lighter soft pink on hover */
        }
        .button-container {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-top: 30px;
        }
        .button {
            padding: 12px 25px;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: bold;
            transition: background-color 0.3s, transform 0.2s;
        }
        .button-primary {
            background-color: #ff6f61; /* Soft pink */
        }
        .button-primary:hover {
            background-color: #e65c5c; /* Darker pink on hover */
            transform: translateY(-2px);
        }
        .button-success {
            background-color: #28a745; /* Keep this as is */
        }
        .button-success:hover {
            background-color: #218838; /* Keep this as is */
            transform: translateY(-2px);
        }
    </style>

</head>
<body>
    <div class="main__container">
        <h2>Exam Results</h2>
        <h3>Hello, {{ complete_name }} &lt;{{ email }}&gt;</h3>

        <h1>Your Score: {{ total_score }} / {{ question_items }}</h1>

        <table>
            <thead>
                <tr>
                    <th>#</th>
                    <th>Question</th>
                    <th>Choices</th>
                    <th>Your Answer</th>
                    <th>Correct Answer</th>
                </tr>
            </thead>
            <tbody>
                {{#questions}}
                <tr>
                    <td>{{ item_number }}</td>
                    <td>{{ question }}</td>
                    <td>
                        <ul>
                            {{#choices}}
                            <li><strong style="color: #ff6f61;">{{ letter }}</strong> {{ choice }}</li>
                            {{/choices}}
                        </ul>
                    </td>
                    <td><strong style="color: #dc3545;">{{ user_answer }}</strong></td>
                    <td><strong style="color: #28a745;">{{ correct_answer }}</strong></td>
                </tr>
                {{/questions}}
            </tbody>
        </table>

        <div class="button-container">
            <a href="/login" class="button button-primary">Restart Exam</a>
            <a href="/examinees" class="button button-success">View All Results</a>
        </div>
    </div>
</body>
</html>
